package client.gui.pages.dashboard.prototype;

/**
 * Enum representing sections in the prototype dashboard.
 * Used by DashboardNavigation to switch between panels.
 */
public enum DashboardSection {
    COLLECTION,
    COMMANDS,
    VISUALIZATION,
    SETTINGS
}
